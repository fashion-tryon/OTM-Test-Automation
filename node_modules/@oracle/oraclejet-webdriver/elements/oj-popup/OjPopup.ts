import {OjPopupBase} from "./OjPopupBase";

/**
 * This is the component WebElement for oj-popup. Retrieve an instance of the class
 * using its convenience function:
 * ```javascript
 * await ojPopup(driver, By.css('oj-popup'))
 * ```
 */
export class OjPopup extends OjPopupBase {
}
