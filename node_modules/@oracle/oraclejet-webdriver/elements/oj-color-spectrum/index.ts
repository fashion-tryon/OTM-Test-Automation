import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjColorSpectrum } from "./OjColorSpectrum";
export { OjColorSpectrum };

/**
 * Create an instance of [[OjColorSpectrum]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojColorSpectrum(driver, By.id('my-oj-color-spectrum'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojColorSpectrum(driver: DriverLike, by: By): Promise<OjColorSpectrum> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjColorSpectrum
  if (!(webEl instanceof OjColorSpectrum)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-color-spectrum>`);
  }
  return webEl;
}

register("oj-color-spectrum", OjColorSpectrum);
