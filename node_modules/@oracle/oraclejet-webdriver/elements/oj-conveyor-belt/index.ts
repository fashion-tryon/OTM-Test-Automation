import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjConveyorBelt } from "./OjConveyorBelt";
export { OjConveyorBelt };

/**
 * Create an instance of [[OjConveyorBelt]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojConveyorBelt(driver, By.id('my-oj-conveyor-belt'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojConveyorBelt(driver: DriverLike, by: By): Promise<OjConveyorBelt> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjConveyorBelt
  if (!(webEl instanceof OjConveyorBelt)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-conveyor-belt>`);
  }
  return webEl;
}

register("oj-conveyor-belt", OjConveyorBelt);
