import {OjTextAreaBase} from "./OjTextAreaBase";
import { By } from "selenium-webdriver";
import { mixinChangeValue } from "../../lib/input-utils";

/**
 * This is the component WebElement for oj-text-area. Retrieve an instance of the class
 * using its convenience function:
 * ```javascript
 * await ojTextArea(driver, By.css('oj-text-area'))
 * ```
 */
export class OjTextArea extends OjTextAreaBase {
}
mixinChangeValue(OjTextArea);
