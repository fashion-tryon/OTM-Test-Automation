import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjRatingGauge } from "./OjRatingGauge";
export { OjRatingGauge };

/**
 * Create an instance of [[OjRatingGauge]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojRatingGauge(driver, By.id('my-oj-rating-gauge'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojRatingGauge(driver: DriverLike, by: By): Promise<OjRatingGauge> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjRatingGauge
  if (!(webEl instanceof OjRatingGauge)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-rating-gauge>`);
  }
  return webEl;
}

register("oj-rating-gauge", OjRatingGauge);
