import {By} from "selenium-webdriver";
import {OjMenuButtonBase} from "./OjMenuButtonBase";

/**
 * This is the component WebElement for oj-menu-button. Retrieve an instance of the class
 * using its convenience function:
 * ```javascript
 * await ojMenuButton(driver, By.css('oj-menu-button'))
 * ```
 */
export class OjMenuButton extends OjMenuButtonBase {
  /**
   * Perform a click on the button
   */
  async doAction() {
    await this.whenReady()
    const b = await this.findElement(By.css('button'))
    await b.click()
  }
}
