import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjNavigationList } from "./OjNavigationList";
export { OjNavigationList };

/**
 * Create an instance of [[OjNavigationList]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojNavigationList(driver, By.id('my-oj-navigation-list'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojNavigationList(driver: DriverLike, by: By): Promise<OjNavigationList> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjNavigationList
  if (!(webEl instanceof OjNavigationList)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-navigation-list>`);
  }
  return webEl;
}

register("oj-navigation-list", OjNavigationList);
