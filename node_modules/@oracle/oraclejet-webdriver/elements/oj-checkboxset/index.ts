import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjCheckboxset } from "./OjCheckboxset";
export { OjCheckboxset };

/**
 * Create an instance of [[OjCheckboxset]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojCheckboxset(driver, By.id('my-oj-checkboxset'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojCheckboxset(driver: DriverLike, by: By): Promise<OjCheckboxset> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjCheckboxset
  if (!(webEl instanceof OjCheckboxset)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-checkboxset>`);
  }
  return webEl;
}

register("oj-checkboxset", OjCheckboxset);
