import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjInputNumber } from "./OjInputNumber";
export { OjInputNumber };

/**
 * Create an instance of [[OjInputNumber]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojInputNumber(driver, By.id('my-oj-input-number'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojInputNumber(driver: DriverLike, by: By): Promise<OjInputNumber> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjInputNumber
  if (!(webEl instanceof OjInputNumber)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-input-number>`);
  }
  return webEl;
}

register("oj-input-number", OjInputNumber);
