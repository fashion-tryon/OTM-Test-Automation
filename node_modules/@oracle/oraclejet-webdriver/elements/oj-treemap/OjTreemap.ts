import {OjTreemapBase} from "./OjTreemapBase";

/**
 * This is the component WebElement for oj-treemap. Retrieve an instance of the class
 * using its convenience function:
 * ```javascript
 * await ojTreemap(driver, By.css('oj-treemap'))
 * ```
 */
export class OjTreemap extends OjTreemapBase {
  // Put overrides here
}