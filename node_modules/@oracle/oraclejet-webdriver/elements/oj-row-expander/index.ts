import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjRowExpander } from "./OjRowExpander";
export { OjRowExpander };

/**
 * Create an instance of [[OjRowExpander]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojRowExpander(driver, By.id('my-oj-row-expander'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojRowExpander(driver: DriverLike, by: By): Promise<OjRowExpander> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjRowExpander
  if (!(webEl instanceof OjRowExpander)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-row-expander>`);
  }
  return webEl;
}

register("oj-row-expander", OjRowExpander);
