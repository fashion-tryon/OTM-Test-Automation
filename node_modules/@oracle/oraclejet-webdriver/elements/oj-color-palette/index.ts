import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjColorPalette } from "./OjColorPalette";
export { OjColorPalette };

/**
 * Create an instance of [[OjColorPalette]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojColorPalette(driver, By.id('my-oj-color-palette'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojColorPalette(driver: DriverLike, by: By): Promise<OjColorPalette> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjColorPalette
  if (!(webEl instanceof OjColorPalette)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-color-palette>`);
  }
  return webEl;
}

register("oj-color-palette", OjColorPalette);
