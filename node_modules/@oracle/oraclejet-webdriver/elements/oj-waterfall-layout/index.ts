import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjWaterfallLayout } from "./OjWaterfallLayout";
export { OjWaterfallLayout };

/**
 * Create an instance of [[OjWaterfallLayout]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojWaterfallLayout(driver, By.id('my-oj-waterfall-layout'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojWaterfallLayout(driver: DriverLike, by: By): Promise<OjWaterfallLayout> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjWaterfallLayout
  if (!(webEl instanceof OjWaterfallLayout)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-waterfall-layout>`);
  }
  return webEl;
}

register("oj-waterfall-layout", OjWaterfallLayout);
