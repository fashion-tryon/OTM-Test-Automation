import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjComboboxOne } from "./OjComboboxOne";
export { OjComboboxOne };

/**
 * Create an instance of [[OjComboboxOne]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojComboboxOne(driver, By.id('my-oj-combobox-one'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojComboboxOne(driver: DriverLike, by: By): Promise<OjComboboxOne> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjComboboxOne
  if (!(webEl instanceof OjComboboxOne)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-combobox-one>`);
  }
  return webEl;
}

register("oj-combobox-one", OjComboboxOne);
