import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjPagingControl } from "./OjPagingControl";
export { OjPagingControl };

/**
 * Create an instance of [[OjPagingControl]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojPagingControl(driver, By.id('my-oj-paging-control'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojPagingControl(driver: DriverLike, by: By): Promise<OjPagingControl> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjPagingControl
  if (!(webEl instanceof OjPagingControl)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-paging-control>`);
  }
  return webEl;
}

register("oj-paging-control", OjPagingControl);
