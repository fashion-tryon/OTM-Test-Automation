import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjButtonsetOne } from "./OjButtonsetOne";
export { OjButtonsetOne };

/**
 * Create an instance of [[OjButtonsetOne]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojButtonsetOne(driver, By.id('my-oj-buttonset-one'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojButtonsetOne(driver: DriverLike, by: By): Promise<OjButtonsetOne> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjButtonsetOne
  if (!(webEl instanceof OjButtonsetOne)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-buttonset-one>`);
  }
  return webEl;
}

register("oj-buttonset-one", OjButtonsetOne);
