import { By } from "selenium-webdriver";
import ojwd, { DriverLike, register } from "../../";
import { OjStatusMeterGauge } from "./OjStatusMeterGauge";
export { OjStatusMeterGauge };

/**
 * Create an instance of [[OjStatusMeterGauge]].  This function will first wait for
 * the element to appear in the DOM before trying to locate it.
 * @example
 * <code>
 * const el = await ojStatusMeterGauge(driver, By.id('my-oj-status-meter-gauge'))
 * </code>
 * @param driver A WebDriver/WebElement instance used to locate the component
 * element.
 * @param by The By locator with which to find the element
 */
export async function ojStatusMeterGauge(driver: DriverLike, by: By): Promise<OjStatusMeterGauge> {
  const webEl = await driver.findElement(by);
  // Check that the element is of type OjStatusMeterGauge
  if (!(webEl instanceof OjStatusMeterGauge)) {
    const tagName = await webEl.getTagName();
    throw Error(`Locator '${by}' located a <${tagName}>, but expected <oj-status-meter-gauge>`);
  }
  return webEl;
}

register("oj-status-meter-gauge", OjStatusMeterGauge);
